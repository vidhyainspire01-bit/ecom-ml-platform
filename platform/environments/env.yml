$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
name: shared-sklearn-env
version: 3
description: Shared training/eval environment used by every model onboarded onto the platform. Bump version here, don't fork a new env per model.
image: mcr.microsoft.com/azureml/openmpi4.1.0-ubuntu20.04
conda_file: conda.yml
